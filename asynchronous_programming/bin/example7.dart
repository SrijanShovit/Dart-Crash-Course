//We want to have a function that takes a start and an end integer and it will simply go from start to that end value
//It will call a function with that value to know whether that value should be included in resulting stream
//then we are gonna pass seprate function pointers to that function

void main(List<String> args) async {}
